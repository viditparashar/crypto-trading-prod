spring:
  autoconfigure:
    exclude: org.springframework.boot.actuate.autoconfigure.security.servlet.ManagementWebSecurityAutoConfiguration
  datasource:
    url: jdbc:mysql://mysql-db:3306/TradingProd?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: viditparashar
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
server:
  port: 8081
logging:
  level:
    root: INFO
